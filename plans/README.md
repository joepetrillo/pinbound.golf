# Implementation Plans

Generated by the improve skill on 2026-07-10 (advisor audit at commit `8a40ad9`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Correct site copy to match confirmed pre-product status | P1 | S | — | DONE |
| 002 | Verification baseline: test runner, first tests, CI | P1 | M | — | TODO |
| 003 | Pin Next.js preview version, prune dead dependencies | P2 | S | — (002 rec.) | TODO |
| 004 | Delete ~1,400 lines of unused waveform/message code | P2 | S–M | — (002 rec.) | TODO |
| 005 | Fix audio bugs: unhandled play() rejection, untracked rAF | P2 | S | — (002 rec.) | DONE |
| 006 | Security headers (CSP/Permissions-Policy) + MDX link hardening | P3 | S | — | TODO |
| 007 | Bring `src/components/ui/**` under lint coverage | P2 | M | 002, 004, 005 | TODO |
| 008 | Single-source sample-call content; reproducible audio pipeline | P2 | M | 001, 002 | TODO |
| 009 | Extract shared `MessageBubble` component | P3 | S | 004 | TODO |
| 010 | README getting-started documentation | P3 | S | — (run last) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002 first (after 001)**: it creates the `bun run test` gate that 003–009's verification steps reference. Plans marked "002 rec." can run without it but lose their regression net.
- **007 after 004 and 005**: 004 deletes ~1,400 lines that would otherwise need lint triage; 005's deliberate fixes should land before lint-driven edits touch the same files.
- **008 after 001**: both edit `src/components/landing/demo.tsx` (001 copy strings, 008 the data array); running 001 first avoids conflicts and 008's drift check anticipates 001's changes.
- **009 after 004**: both edit `src/components/ui/message.tsx`.
- **001 is independent and the highest-leverage item**: the product brief (`docs/pinbound-product-context.md:159-167`) gates driving any traffic on these corrections.
- **010 last**: it documents whatever `package.json` scripts exist after the other plans.

## Findings considered and rejected

- **Reading-time `readFileSync` duplication** (`src/lib/blog.ts:29-35` re-reads each post from disk instead of using fumadocs' parsed content): pages are statically cached; the cost is negligible and the fix depends on unstable fumadocs preview APIs. Not worth doing now.
- **MDX link "tabnabbing"**: no `target="_blank"` exists anywhere in the MDX renderer, so the reported tabnabbing risk was overstated; the residual scheme-allowlist hardening was folded into plan 006.
- **Moving section copy into `src/lib/site.ts`**: single-use copy co-located in its section component is the repo's working convention; only cross-page strings belong in `site.ts`. Not a finding.
- **Three.js/orb bundle weight**: already handled well — `next/dynamic` with `ssr: false` and a skeleton fallback (`demo.tsx:26-35`). No action.
- **README describing the future SaaS**: intentional per `docs/pinbound-product-context.md` (the repo becomes the full product); not a stale doc.
- **Blog-post marketing tone** (`content/blog/*` present-tense feature descriptions): real but requires the maintainer's voice; recorded as deferred follow-up in plan 001's maintenance notes rather than an executor plan.
- **Direction findings** (personalized-recording pipeline, real lead capture, live ElevenLabs demo, mock tee-sheet adapter spike): presented 2026-07-10; maintainer chose to defer all four this round.
