# Implementation Plans

Generated by the `improve` skill on 2026-07-22. This directory is used instead of `plans/` because the repository's existing `plans/` directory contains an unrelated onboarding plan and currently has uncommitted user changes.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Migrate the blog to Satteri local Markdown without changing behavior | P1 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with a one-line reason) | REJECTED (with a one-line rationale)

## Dependency notes

- Plan 001 is self-contained. Its first step captures the current blog build as the characterization baseline before any migration changes are made.

## Findings considered and rejected

- Use `dynamicLoader()` in production solely because it is the documentation default: rejected because production content does not revalidate today. The plan uses a production `staticSource()` snapshot and a development-only `dynamicLoader()` plus watcher.
- Remove `@types/mdx`: rejected because `src/mdx-components.tsx` imports `MDXComponents`, and the Satteri renderer accepts that same type.
- Change `src/mdx-components.tsx`: rejected because its link and scrollable-table overrides are already compatible with Satteri's `render(components)` API.
- Add `dynamicParams = false`: rejected because the installed Next.js Cache Components documentation says that route-segment option is unavailable when `cacheComponents` is enabled.
